s = """           Python – динамічна інтерпретована об’єктно-орієнтованаскриптова мова програмування 
із строгою динамічною типізацією. Розроблена в 1990 році голандським програмістом Гвідо ван Россумом .
Python – багатоцільова мова програмування, яка дозволяє писати код, що добре читається. Відносний лаконізм мови Python
 дозволяє створити програму, яка буде набагато коротше свого аналога, написаного на іншій мові.    """
# Светюха Катерина:
s1 = s.strip()  # Видалення пробілів
r = s1.replace("Python", "Пайтон")  # Заміна "Python" на "Пайтон"
l = len(s1)  # Довжина рядка
s2 = s.split()  # Розбиття рядка на список слів

# Кратенко Руслан:
joined_s = " ".join(s2)  # З'єднання списку слів у рядок
slice_s = s[:50]  # Отримання перших 50 символів
count_word = s.count("мова")  # Підрахунок входжень слова "мова"

# Спориш Владислав:
upper_s = s.upper()  # Перетворення тексту на верхній регістр
find_substring = s.find("Гвідо")  # Пошук слова "Гвідо"
split_by_dash = s.split("–")  # Розбиття рядка за символом "–"

# Литвиненко Роман:
startswith_python = s.startswith("Python")  # Перевірка, чи починається рядок з "Python"
punctuation_replaced = s.replace(".", " ").replace(",", " ")  # Заміна пунктуації на пробіли
lower_s = s.lower()  # Перетворення тексту на нижній регістр

# Дворнік Олександр:
reversed_s = s[::-1]  # Реверс рядка
vowels_removed = "".join([char for char in s if char not in "AEIOUaeiouАЕЄИІОУЯЮЇаеєиіоуюяї"])  # Видалення голосних
count_a = s.count("а")  # Підрахунок кількості символу "а"

# Виведення результатів
print("Рядок після видалення пробілів в початку та в кінці:\n", s1)
print("Рядок після заміни слова Python:\n", r)
print("Довжина рядка:", l)
print("Перетворення рядка в список слів:\n", s2)

print("З'єднаний рядок з списку слів:\n", joined_s)
print("Перші 50 символів рядка:\n", slice_s)
print("Кількість входжень слова 'мова':", count_word)

print("Рядок у верхньому регістрі:\n", upper_s)
print("Індекс підрядка 'Гвідо':", find_substring)
print("Розбиття рядка за '–':\n", split_by_dash)

print("Чи починається рядок з 'Python':", startswith_python)
print("Рядок після заміни пунктуації на пробіли:\n", punctuation_replaced)
print("Рядок у нижньому регістрі:\n", lower_s)

print("Реверс рядка:\n", reversed_s)
print("Рядок без голосних букв:\n", vowels_removed)
print("Кількість входжень символу 'а':", count_a)
